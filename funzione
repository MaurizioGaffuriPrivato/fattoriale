long long fattoriale(long long numero)
{
  double banana;
  if(numero<0)
         return -1;
  else
         if(numero==0) 
                return 1;
         else
                return numero*fattoriale(numero-1);
}
